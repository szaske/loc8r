<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.loc8r.seattle">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeattlePassport">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="Seattle Passport"/>
        <activity android:name=".tests.PinOnMap"/>
        <activity android:name=".tests.mapFeatureCollection"/>
        <activity android:name=".tests.mapPlusRecycler"/>
        <activity android:name=".tests.mapDataFromFirebase"/>
        <activity android:name=".activities.ExploreSeattle"/>
        <activity android:name=".activities.POIDetailActivity"/>

        <!-- <service -->
        <!-- android:name=".services.LocationBaseService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true"> -->
        <!-- </service> -->
        <activity android:name=".activities.FontsActivity">
        </activity>
        <activity android:name=".activities.LoggedInActivity">
        </activity>
    </application>

</manifest>